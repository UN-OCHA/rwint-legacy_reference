
Legacy Reference
----------------

Field module for Drupal 7.x

Legacy Reference module defines a field that can contain one or more legacy IDs.
These legacy ids can be resolved to Drupal references like taxonomy term
reference or node reference (todo) if an entity with the same legacy ID
feed_item.guid ID can be found.
